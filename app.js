const { createApp } = Vue;
createApp({
    data() {
        return {
            message: 'prima lezione di Vue lunedi 29 gennaio 2024',
            img: './gatto.jpeg',

        };
    }
}).mount('#app');